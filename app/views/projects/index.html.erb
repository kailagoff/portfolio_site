<div class="well">
  <h5>Projects:</h5>
  <ul>
    <% @projects.each do |project| %>
       <li><%= link_to image_tag(project.image.url(:large)), project_path(project) %></li>
    <% end %>
  </ul>
</div>
